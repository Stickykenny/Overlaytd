import { CellStyle, themeAlpine, ValueFormatterParams } from "ag-grid-community";
import { RowModel } from "src/app/models/RowModel";

export const GridUserConfig = {
  idCellStyle: { color: "#00008B", backgroundColor: "#eeeeeeff" },
};
